
new jackpot todos
	deposit winnings to user balance

hls stream todos
	update hls.js
	configure hls.js to narrow live edge

app_tools todos
	crud handler permissions
	app service descriptors

	exports
	docs

	file logs
		log to /var/log
		periodic cleanup? built in linux stuff?
	gs_main
		convert to ts
		create live file cache
		implement all sports like tennis
	hls_sport
		ts conversion
		implement all sports like tennis

muhammet new website todos
	blackjack and ratepoker lack alert
	add new games
	add okey tavla holdem

app_tools improvements
	redis instead of memory cache
	threaded work consumer?

gs_main
	inplay file cache
	convert to typedoc
	implement other sports

hls_sport
	convert to typedoc

implement ahmed's db backup code

hlts live event betting
	bet types
		win bet: who will finish first
		place bet: first or second, paid with each way odds
		exacta bet: regular forecast betting
		trifecta bet: regular tricast betting
		quinella bet: place on 2 racers to finish first and second (place 2 exacta bets)
		betting without the favorite?: win or each way bets, settled ignoring the position of the favorite, how to calculate these odds? or scrape from boylesports
		inside vs outside: bet on trap 1,2,3 vs 4,5,6, can be calculated or scraped
		odds vs evens: bet on odd vs even numbered traps, can be calculated or scraped
	non-runner issues
		if bet placed on trap, pay if substitute wins
		if bet placed on name, refund
		if trap didn't run, refund
		if starting odds changed because of non-runner then pay everyone with new starting odds?
		if tied, then divide the stake by number of tied racers and pay with normal odds, no refunds
		if race abandoned/rescheduled refund all bets
	scrape
		https://www.boylesports.com/sports/greyhounds

improve new admin
	talk to ceyhun

beat the guardian (yildiz then osman)
	balance the game
	create concept art with ai
	osman creates the game


shop easy betting system
	horse and dog racing
	sports betting
	new domain that shows all odds
	customer prepares a temp coupon on their own phone/kiosk
		customer can set minimum rates for each choice or the total coupon rate
		if any rates fall below the minimum at the time of payment then the operator is alerted and additional confirmation is required from customer
	customer is given a 6-digit xxx-xxx numeric code or barcorde or qr code for depositing bet in the shop
	shop operator takes the money and finalizes the coupon, gives customer a regular coupon printout or the customer can view the finalized bet on their phone
		no printout option for live betting since the deposit delay will slow things down too much
		user can check the status of their bet through their phone using the qr code
		or if the user adds their phone number to the coupon we can send them an approved/rejected coupon link
	this forms the basis of the new website
		mobile friendly and without rng games in the beginning
		at later stages add in the old games
		then rip from another source and add in live casino games
	if logged in to the website the users can watch events that they bet on
		if the user is marked as vip, they can watch all events, get higher max limits, reduced/no live betting delay, etc

new website ui
	need db for race betting coupons, single race coupons only, no mixed betting
	can create temp coupon and pay at a shop or login and pay with account balance
	temp coupons and secret keys get saved on the device local storage, barcodes/qr codes/numeric codes can be used to pay rejected/cancelled/won coupons
kiosk ui
	shows have kiosk users that need to be logged in
	user can either take a photo of the qr code / numeric code on the screen or print out from kiosk
	the temp coupon code is then queried and a printout is given to the user even if the coupon isn't finalized
	if the user entered a phone number then rejections/cancellations/wins are texted/pushed/whatsapped to the user
	the user can either use the coupon or the text message to get paid
coupon qr urls
	https://coupons.nicobet.com/selection?id=xxx-xxx
		type: sports/racing
		bet_stake: xxx.xx
		choices: [{ id: xxxxxx, min_rate: xxxx.xx, must_have: true }]
		auto_combinations: "S/A5/etc" // sports betting only
		total_rate: xxxxxx.xx
		min_rate: xxxxxx.xx
		must_have_all: true
		phone?: xxx-xxx-xx-xx
	https://coupons.nicobet.com/coupon?id=xxx-xxx-xxx-xxx&key=xxx-xxx
		same as above +
		place_date
		accept_date
		status: "Initializing/Open/Rejected/Lost/Won/Cancelled/Paid back"
		bet_return: xxxxxx.xx
digital coupon
	a link is given to the customer via sms or whatsapp to view the status of the coupon
	the customer selects if they prefer a print or digital coupon during bet formation
	coupons containing live betting options must be digital
	when the customer cashes in a digital coupon, a coupon is printed for the shop for their records

finish ts conversion (yildiz)
	docs
	hls_game

beat the guardian (interns and osman)
	balance the game
	create concept art with ai
	osman creates the game

improve new admin (yasin)
	
slots and bonanza
	convert to reel logic like in super hot

game error check job
	check hlbs payments to see if all have redemptions
		if no redemption then refund

client side
	winners list
	game resumption

spin the wheel game

video poker
	leftovers go into leftover asset (per bet option)
	job takes from leftover asset and creates more prizes in the leftover pool, this pool never detaches

games side
	maintenance message
	create a status endpoint in the hls_game.main app

sports betting
	refactor ls_main, gs_main, and hls_sport
	move schemas to main data center
	develop admin to display/adjust matches/bookmakers

entity_crud_service
	add returning support
	add functional update support
	add a transactional action that can do insert/update in a transaction, and still create the change logs

20 dazzling hot
	https://egt-digital.com/game/20-dazzling-hot/
	no wild, single scatter
	clover chance jackpot feature
		player collects chests until one of the jackpots are won
		jackpot is triggered randomly after the prev hand is paid